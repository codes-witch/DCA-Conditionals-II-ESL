% !TEX root = ../main.tex
\section{Methods}


\subsection{Data: The EFCAMDAT}

 The EF-Cambridge Open Language Database (EFCAMDAT), first presented by \citet{geertzen2013automatic}, is a large-scale learner corpus containing writings from the online English school \textit{Englishtown}.
 In their courses, each level corresponds to eight pedagogical units, after each of which students receive a prompt for a writing task that they must complete in order to advance. 
 The corpus comprises responses to these tasks in their original and corrected forms, along with metainformation about the text and the author (learner ID, country of origin, grade given by a teacher, etc.).
 An advantage of the EFCAMDAT, beyond being generally available for researchers at all stages, is the fact that \textit{Englishtown} levels are aligned with the CEFR, making it possible to do a broad range of descriptive analyses of L2 English at globally understood proficency levels.\footnote{The availability of large-scale, CEFR-aligned corpora is more limited than one might expect. The most well-known English learner corpus is the Cambridge Learner Corpus (CLC), whose access is highly restricted.}

The version of EFCAMDAT used for this study contained 620{,}206 learner writings in total. 
However, it was unlikely for second conditionals to be elicited by most prompts; therefore, after qualitatively inspecting the task prompts, I decided on a subset of 20 tasks whose responses were most likely to contain the construction. 
The selected units and corresponding writing topics are outlined in Table \ref{tab:topics}, totaling 33{,}496 student writings and 4{,}402{,}652 words.

\input{content/tables/topics.tex}

\subsection{Extracting conditionals}

All corrected learner writings in the subcorpus were automatically parsed using Stanza \citep{qi2020stanza}. 
Using the corrected texts instead of the original ones is justified by the fact that accuracy does not factor into this study: 
the goal is not to asses the grammatical proficiency of students, but rather to explore their lexical preferences is a specific slot in different levels.
This gives us the ability to use the corrected texts that are present in the corpus, ensuring more robust results of automatic Natural Language Processing (NLP) tools.\footnote{However, given previous robustness studies of NLP tools on L2 English \citep{geertzen2013automatic}, similar results would be expected when running the analysis on the original learner texts.}

The parsed documents were scanned for type II conditionals using both Universal Dependency (UD) relations and part-of-speech (POS) information. 
Only sentences containing the word "if" as a conditional marker (headed by an \texttt{advcl} UD tag) and not as an interrogative marker headed by a \texttt{ccomp} UD tag, as in Figure \ref{noncondif}.
Then, the script ensures that the verb in the protasis is in the past tense and the apodosis contains a valid modal verb, thus filtering out other types of conditionals. 
For this study, I excluded sentences that expressed their conditional value through means other than the grammatical marker "if" (e.g. subject-auxiliary inversion) and sentences containing a modal verb in the protasis (e.g. \textit{If I could go anywhere in the world, I'd go to China.}).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{images/noncond_if.png}
    \caption{UD parse of a sentence with a non-conditional \textit{if}}
    \label{noncondif}
\end{figure}

Finally, all conditionals that fit all requirements were compiled into a CSV file with the full sentence, the lemma filling the verbal slot in the protasis and metadata about the writing prompt and response, such as the \textit{Englishtown} level the task stemmed from and the topic ID. 

\subsection{Calculating dispersion across tasks}

\subsection{Distinctive Collexeme Analysis}

\subsection{Lexical variability}
